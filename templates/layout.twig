<!doctype html>
<html lang="et">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title|default('Booking') }}</title>
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; background:#fff; margin:0; }
    .wrap { padding: 28px; }
    .card { width: 640px; max-width: 100%; background:#fff; border:1px solid #e6e6e6; border-radius: 10px; padding: 26px; box-shadow: 0 0 0 rgba(0,0,0,0); }
    h1 { margin: 0 0 18px; font-size: 34px; }
    h2 { margin: 22px 0 8px; }
    label { font-weight: 600; display:block; margin: 14px 0 6px; }
    select, input { width: 100%; padding: 10px; border: 1px solid #cfcfcf; border-radius: 4px; }
    .btn { display:inline-block; padding: 10px 14px; border:1px solid #cfcfcf; border-radius:4px; background:#f4f4f4; cursor:pointer; }
    .btn:disabled { opacity:0.45; cursor:not-allowed; }
    .grid { display:flex; flex-wrap:wrap; gap:12px; margin-top: 14px; }
    .slotform { margin:0; }
    .muted { color:#666; margin: 0 0 10px; }
    a { color: #6a1bbf; text-decoration: underline; }
    ul { margin: 8px 0 0 18px; }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      {% block content %}{% endblock %}
    </div>
  </div>
</body>
</html>
